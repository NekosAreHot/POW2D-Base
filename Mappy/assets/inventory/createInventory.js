Game.createInventory = function(numberofbuttons,buttonObject,selector){
	var num = numberofbuttons;
	var textObj = buttonTextObject;
	var enableSelector = selector;
	
	//check to see if enableSelector is on
	if(enableSelector){
		
	}else{
		//Get the num of buttons and display them
		
		
	}
	
}